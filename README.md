# bt4103Team12

## Setup Instructions

### Frontend
1. `npm i` (first time ONLY)
2. `npm run dev`

### Backend
Create a virtual environment first, then activate it:
1. `pip install -r requirements.txt` (first time ONLY)
2. `uvicorn main:app --reload --port 8000`
